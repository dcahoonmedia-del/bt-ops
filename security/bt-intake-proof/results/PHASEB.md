# Phase B — cloud-hosted intake proof

VM `bt-intake-cloud` is on-spec (e2-small, us-east1-c, receiver SA, 20 GB). Receiver service was installed and `active` at 2026-09-12 03:19:32Z. No JSON key.

SSH via `authorized_keys` was overwritten by the GCE guest agent. Need a persistent instance SSH key to finish live verification and Mac-off tests.
