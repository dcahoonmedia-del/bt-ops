# Guarded deploy — CASEMGR initial-review delivery

Deploy transaction only. **Do not send mail from deploy.**
Do not run `recover-initial-case-review`.
Do not resend `BT-INTAKE-PROOF-CASEMGR-PHONE-7C92`.
Do not reset SQLite. Do not recover `--execute`.
Do not repeat action 2 `--verify-recipient`.
Do not touch `BTC-contactus-1a096d60643b3b1a`.

Customer sends stay OFF. Broad capture stays OFF.

Use the TRANSFER card in `results/desk-roundtrip-release/TRANSFER` after the
immutable tarball is pinned.

After deploy, wait for Daniel / Codex review before the bounded recovery in
`RECOVERY.md`. A later new CASEMGR inbound should produce one CASE packet
automatically. That is not proven until a live host cycle is observed.
